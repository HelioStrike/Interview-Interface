>> CREATE TABLE users(username VARCHAR(100), password VARCHAR(32), created_on TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP);

>> CREATE TABLE interviews_master(student_id VARCHAR(1000), interview_id INT, description VARCHAR(1000), question_bank_id INT, bank_id INT, start_time INT, end_time INT, status varchar(20), created_by VARCHAR(100), created_on TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP);

>> CREATE TABLE questions_master(id INT, question VARCHAR(1000), answer VARCHAR(10000), type VARCHAR(20), created_by VARCHAR(100), created_on TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP);

>> CREATE TABLE question_banks_master(name VARCHAR(200), id INT, description VARCHAR(1000), created_by VARCHAR(100), created_on TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP);

>> CREATE TABLE question_to_bank(question_id INT, bank_id INT);
